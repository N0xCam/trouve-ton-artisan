import { FilterVySpecialyPipe } from './filter-by-specialy.pipe';

describe('FilterVySpecialyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVySpecialyPipe();
    expect(pipe).toBeTruthy();
  });
});
