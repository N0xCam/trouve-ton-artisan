import { BatimentPipe } from './batiment.pipe';

describe('BatimentPipe', () => {
  it('create an instance', () => {
    const pipe = new BatimentPipe();
    expect(pipe).toBeTruthy();
  });
});
